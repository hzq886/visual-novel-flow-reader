/**
 * validate — data/scenes/*.json の全 beat で bg.file / sprite.body / voice.file の未解決を集計し、
 * flow.json の全シーンコードが原テキストに実在するか相互照合する。未解決があれば exit 1。
 * 実装は VN-4（validateFlow）で行う。
 */
console.log(
  '[validate] TODO: VN-4 で validateFlow を実装（未解決参照の検出・flow とシーンの相互照合）',
)
