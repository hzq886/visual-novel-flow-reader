/**
 * build-flow — route_map.html / build_ayan_end1.py から暫定 data/flow.json を起こす（二次ソース）。
 * 一次ソース（md_scr.med の SMAIN + select オペコード再抽出）は extract-flow.py（VN-11）。
 * 実装は VN-9 で行う。
 */
console.log(
  '[build-flow] TODO: VN-9 で route_map/build_ayan から暫定 flow.json を生成（一次は extract-flow.py / VN-11）',
)
