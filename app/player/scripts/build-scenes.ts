/**
 * build-scenes — data_extract/text のシーンファイルに parseScene を適用し、
 * data/scenes/<code>.json を生成する（--scene <code> で1本指定可）。
 * 実装は VN-3（parseScene）＋ VN-4（resolve*）で行う。
 */
console.log(
  '[build-scenes] TODO: VN-3/VN-4 で parseScene + resolve* を適用し data/scenes/*.json を生成',
)
