/**
 * build-defs — _SPRSET.txt / _BGSET.txt をパースして
 * data/sprites.json / data/backgrounds.json（ラベル→コード解決テーブル）を生成する。
 * 実装は VN-2（parseSprset / parseBgset）で行う。
 */
console.log(
  '[build-defs] TODO: VN-2 で parseSprset / parseBgset を実装し sprites.json / backgrounds.json を生成',
)
